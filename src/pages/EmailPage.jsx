import React from "react";
import styled from "styled-components";

const EmailPage = () => {
  return <EmailPageContainer>EmailPage</EmailPageContainer>;
};

const EmailPageContainer = styled.div``;

export default EmailPage;
